import Heading from "@/components/elements/Heading";

export default function StillsSlideCreator() {
    return <Heading text="Not yet available." size={3} className='text-neutral-600 font-normal' />
}